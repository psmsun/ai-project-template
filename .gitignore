# Dependencies
node_modules/
.venv/
__pycache__/

# Build output
dist/
build/
.next/
*.tsbuildinfo

# Env / secrets
.env
.env.local
.sandcastle/.env

# Sandcastle runtime
.sandcastle/logs/
.sandcastle/worktrees/

# skills CLI generated mirror (canonical copies live in .claude/skills/; keep skills-lock.json tracked)
.agents/
.factory/

# Note: template.config.json (the stack record written by init) IS committed so the repo is
# self-describing — do not ignore it. Only template.config.example.json is removed by init.

# OS / editor
.DS_Store
